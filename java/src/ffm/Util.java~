package ffm;

public class Util
{
    static public String joinArray(String[] requests)
    {
        String request = "";
        for(int i = 0; i < requests.length; i++){
            request += requests[i] + "\r\n";
        }
        return request;
    }
}